import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10,
        borderRadius: 5,
        margin: 5,
    },
    text: {
        color: 'white',
        fontSize: 12,
    },
});
export default styles;